<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://gohugo.io/" version="0.163.3">Hugo</generator><title type="html">Migration on Jsonnet-as-a-Service</title><link href="https://jaas.projects.metio.wtf/tags/migration/" rel="alternate" type="text/html" title="html"/><link href="https://jaas.projects.metio.wtf/tags/migration/index.xml" rel="alternate" type="application/rss+xml" title="rss"/><link href="https://jaas.projects.metio.wtf/tags/migration/atom.xml" rel="self" type="application/atom+xml" title="atom"/><updated>2026-07-02T07:56:37+00:00</updated><author><name>metio.wtf</name></author><id>https://jaas.projects.metio.wtf/tags/migration/</id><entry><title type="html">Upgrading</title><link href="https://jaas.projects.metio.wtf/reference/upgrading/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://jaas.projects.metio.wtf/reference/helm-values/?utm_source=atom_feed" rel="related" type="text/html" title="Helm chart values"/><link href="https://jaas.projects.metio.wtf/get-started/kubernetes/?utm_source=atom_feed" rel="related" type="text/html" title="Kubernetes"/><link href="https://jaas.projects.metio.wtf/running/disaster-recovery/?utm_source=atom_feed" rel="related" type="text/html" title="Backup and disaster recovery"/><link href="https://jaas.projects.metio.wtf/reference/configuration/?utm_source=atom_feed" rel="related" type="text/html" title="Configuration reference"/><link href="https://jaas.projects.metio.wtf/running/operations/?utm_source=atom_feed" rel="related" type="text/html" title="Operations"/><id>https://jaas.projects.metio.wtf/reference/upgrading/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:54:29+02:00</updated><content type="html"><![CDATA[<blockquote>Actions required when upgrading JaaS between releases — a release with no section here needs no migration.</blockquote><p>This page lists the actions required when upgrading JaaS. A release with no
section here needs no migration — a plain <code>helm upgrade</code> (or new image tag)
suffices.</p>
<h2 id="2026615">2026.6.15</h2>
<p>JaaS gains an opt-in operator mode that watches <code>JsonnetSnippet</code> and <code>JsonnetLibrary</code> CRDs (<code>jaas.metio.wtf/v1</code>) and publishes evaluated results as Flux <code>ExternalArtifact</code> resources. <strong>Operator mode ships as GA on this release</strong> — the CRDs are at <code>v1</code> (no <code>v1alpha1</code>/<code>v1beta1</code> ever published), and the wire contracts in scope are committed: every <code>Reason*</code> constant on the Ready condition, the <code>ErrCode*</code> HTTP error codes, the <code>ExternalArtifact</code> spec/status shape, the chart values keys under <code>operator.*</code>, and the <code>JsonnetSnippet</code> / <code>JsonnetLibrary</code> field set. Breaking changes to any of these require a new CRD version + deprecation window, never an in-place rename. See the <a href="/api/">API reference</a>
 for the committed field sets.</p>
<p>The container image now ships from GitHub Container Registry: <code>ghcr.io/metio/jaas</code> (previously <code>docker.io/metio/jaas</code>). The chart&rsquo;s <code>image.registry</code> default moves to <code>ghcr.io</code> to match, so a plain <code>helm upgrade</code> pulls from the new location automatically. Pin-by-registry installs (<code>--set image.registry=docker.io</code>) and any infrastructure that mirrors or allow-lists the image by registry must repoint at <code>ghcr.io</code>; the Docker Hub repository is no longer published.</p>
<p>The image is now a multi-arch manifest covering <code>linux/amd64</code>, <code>arm64</code>, <code>arm/v7</code>, <code>ppc64le</code>, <code>riscv64</code>, and <code>s390x</code>, so the operator schedules onto any node architecture (and matches the JOI library images it consumes). The runtime base also moves from <code>cgr.dev/chainguard/static</code> to <code>gcr.io/distroless/static:nonroot</code> — both are shell-less static bases, so behavior is unchanged, but image-signature/SBOM tooling that pinned the chainguard base digest needs updating.</p>
<p>The Helm chart now lives in the <a href="https://github.com/metio/helm-charts/tree/main/charts/jaas">metio/helm-charts</a>
 monorepo and is published at <strong><code>oci://ghcr.io/metio/helm-charts/jaas</code></strong> (previously <code>oci://ghcr.io/metio/jaas</code>, released from this repo). Repoint your <code>helm install</code> / <code>helm upgrade</code> source — for example <code>helm upgrade --install jaas oci://ghcr.io/metio/helm-charts/jaas …</code>. The chart&rsquo;s templates, values, and CRD set are unchanged by the move; only the artifact&rsquo;s location differs. Chart <code>version</code> and <code>appVersion</code> now advance independently of the jaas binary, since the chart is released on the helm-charts repo&rsquo;s own cadence.</p>
<p>The HTTP-only evaluator continues to work unchanged — operators that don&rsquo;t enable the new mode see no behavioral difference.</p>
<p>A new flag <code>--max-concurrent-evals</code> caps in-flight Jsonnet evaluations across both surfaces; excess requests return HTTP <code>503 evaluation_unavailable</code> or — on the operator path — a <code>Warning EvalUnavailable</code> event plus <code>RequeueAfter</code>. The default (<code>max(GOMAXPROCS*4, 16)</code>) is sized to bound worst-case goroutine pile-up under a runaway snippet; set to <code>0</code> to disable the gate entirely. The chart&rsquo;s <code>arguments.maxConcurrentEvals</code> value threads through to the flag. A new wire-stable HTTP error code <code>evaluation_unavailable</code> joins the existing four (<code>method_not_allowed</code>, <code>snippet_not_found</code>, <code>evaluation_timeout</code>, <code>evaluation_failed</code>) — programmatic callers that switch on the error code should add a branch for 503/<code>evaluation_unavailable</code>; treat it as transient and retry with backoff.</p>
<p>To enable the operator mode, pass <code>--enable-flux-integration</code> along with <code>--storage-path</code> (a writable directory for tarballs) and <code>--storage-base-url</code> (the URL prefix downstream Flux consumers will dereference). The operator opens a third HTTP server at <code>--storage-listen-address:--storage-port</code> (defaulting <code>0.0.0.0:8082</code>) to serve artifacts.</p>
<p>The validating admission webhook is independently opt-in via <code>--enable-webhook</code> and requires a TLS cert/key in <code>--webhook-cert-dir</code>. The reconciler enforces the same ext-var conflict invariant as a fallback, so the webhook is purely a UX improvement on <code>kubectl apply</code>.</p>
<p>CRDs ship with the helm chart under its <code>templates/crd-*.yaml</code>. <code>helm install</code> provisions them on first apply and <code>helm upgrade</code> applies any schema changes automatically — no manual <code>kubectl apply</code> of CRDs required. Each CRD template carries a <code>helm.sh/resource-policy: keep</code> annotation so <code>helm uninstall</code> does NOT wipe the CRDs (or every <code>JsonnetSnippet</code> / <code>JsonnetLibrary</code> bound to them). An operator who genuinely wants to remove the CRDs runs <code>kubectl delete crd jsonnetsnippets.jaas.metio.wtf jsonnetlibraries.jaas.metio.wtf</code> by hand. The <code>ValidatingWebhookConfiguration</code>, <code>Service</code>s, RBAC, and the optional cert-manager <code>Certificate</code> for the webhook are rendered when <code>operator.enabled</code> / <code>operator.webhook.enabled</code> are toggled in the chart values.</p>
<p>A new flag <code>--artifact-gc-grace</code> (default <code>5m</code>) sets the minimum time a superseded artifact revision is retained before storage GC removes it. Closes the pin→fetch race in which a Flux consumer reads <code>status.artifact</code> a moment before the operator garbage-collects the superseded revision and then 404s on the URL. Supersession time is derived from on-disk storage metadata so the window survives operator restarts. <code>0</code> restores the prior eager-prune behavior; the snippet teardown path (finalizer <code>Withdraw</code>) is unaffected. See <a href="/operator/storage-and-ha/#revision-retention-and-rollback">Revision retention and rollback</a>
 for when to leave the default vs raise <code>spec.history</code> for deliberate rollback retention.</p>
<p>The <code>ExternalArtifact.spec.sourceRef</code> JaaS writes — <code>{apiVersion: jaas.metio.wtf/v1, kind: JsonnetSnippet, name: &lt;snippet name&gt;}</code> in the snippet&rsquo;s own namespace — is now documented as a public contract. Producer-aware consumers (<code>stageset-controller</code> and others) reverse-resolve <code>JsonnetSnippet</code> references through exactly this triple; renaming a field, splitting <code>apiVersion</code> into <code>group</code>/<code>version</code>, or moving the back-pointer out of <code>spec.sourceRef</code> is a breaking change. No spec change in this release — the shape was already produced as documented — but the commitment to it is now load-bearing.</p>
<h3 id="known-limits-at-ga">Known limits at GA</h3>
<ul>
<li><strong><code>vm.EvaluateFile</code> has no context cancellation</strong> (go-jsonnet upstream constraint). An evaluation that exceeds <code>--evaluation-timeout</code> keeps a goroutine alive until the snippet finishes naturally — observable via the <code>jaas_eval_outstanding_timed_out</code> Prometheus gauge. <code>--max-concurrent-evals</code> caps how many such goroutines can pile up at once; the cap, combined with <code>--max-stack</code> and <code>--evaluation-timeout</code>, bounds the worst-case blast radius. The opt-in PrometheusRule template ships <code>JaaSEvalSaturation</code> + <code>JaaSEvalRejected</code> alerts; <a href="/runbooks/eval-saturation/">Eval saturation</a>
 covers the runaway-snippet vs. genuine-load diagnosis paths.</li>
<li><strong>Watch-namespace RBAC pivots automatically</strong> when <code>operator.watchNamespaces</code> is set. The cluster-scoped <code>operator-cluster</code> role (CRDs + optional VWC) stays bound cluster-wide. The <code>operator-tenants</code> role (snippets, libraries, ExternalArtifact, Flux sources, SAs, token mint, events) is bound either cluster-wide (default, when <code>watchNamespaces</code> is empty) or via one <code>RoleBinding</code> per listed namespace (when <code>watchNamespaces</code> is non-empty). Both the deployment&rsquo;s <code>-watch-namespaces</code> arg and the rendered RBAC are driven from the same value, so multi-tenant installs running disjoint operator instances per tenant-group get tight RBAC + cache scoping automatically.</li>
<li><strong>Single-replica is the supported topology by default.</strong> Leader election ships on so scaling out works without double-reconciliation, but the chart-default storage backend is <code>local</code> with an emptyDir / RWO PVC — both effectively single-pod. Multi-replica HA needs <code>operator.storage.backend: s3</code> plus a shared S3 bucket.</li>
</ul>
<h2 id="2026525">2026.5.25</h2>
<p>The labels in the Deployment and PDB <code>spec.selector.matchLabels</code> have changed. Kubernetes treats those fields as immutable, so <code>helm upgrade</code> from an older chart version will fail with <code>field is immutable</code>.</p>
<p>Delete both resources before upgrading:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl -n &lt;namespace&gt; delete deployment jaas
</span></span><span class="line"><span class="cl">kubectl -n &lt;namespace&gt; delete pdb jaas --ignore-not-found
</span></span></code></pre></div><p>Expect a brief outage while the new Pod comes up.</p>
]]></content><category scheme="https://jaas.projects.metio.wtf/tags/installation" term="installation" label="installation"/><category scheme="https://jaas.projects.metio.wtf/tags/upgrade" term="upgrade" label="upgrade"/><category scheme="https://jaas.projects.metio.wtf/tags/migration" term="migration" label="migration"/><category scheme="https://jaas.projects.metio.wtf/tags/helm" term="helm" label="helm"/></entry></feed>