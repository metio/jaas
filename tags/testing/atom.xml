<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://gohugo.io/" version="0.163.3">Hugo</generator><title type="html">Testing on Jsonnet-as-a-Service</title><link href="https://jaas.projects.metio.wtf/tags/testing/" rel="alternate" type="text/html" title="html"/><link href="https://jaas.projects.metio.wtf/tags/testing/index.xml" rel="alternate" type="application/rss+xml" title="rss"/><link href="https://jaas.projects.metio.wtf/tags/testing/atom.xml" rel="self" type="application/atom+xml" title="atom"/><updated>2026-07-24T07:17:17+00:00</updated><author><name>metio.wtf</name></author><id>https://jaas.projects.metio.wtf/tags/testing/</id><entry><title type="html">Building and Testing</title><link href="https://jaas.projects.metio.wtf/contributing/building/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://jaas.projects.metio.wtf/contributing/ci-and-release/?utm_source=atom_feed" rel="related" type="text/html" title="CI and Releases"/><link href="https://jaas.projects.metio.wtf/integrations/claude-code/?utm_source=atom_feed" rel="related" type="text/html" title="Claude Code skill"/><id>https://jaas.projects.metio.wtf/contributing/building/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-07-16T23:00:41+02:00</updated><content type="html"><![CDATA[<blockquote>Build JaaS and run its full test suite through the flake&rsquo;s development shell.</blockquote><p>The host needs no Go toolchain. Every build and test command runs through the
development shell that <code>flake.nix</code> defines, with <code>flake.lock</code> pinning each tool
to an exact version:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command &lt;command&gt;
</span></span></code></pre></div><p>CI runs the same shell, so a gate that is green locally is green there by
construction. Run <code>nix develop</code> on its own to drop into an interactive shell and
invoke the tools bare.</p>
<p>The shell carries the Go toolchain, the static-analysis suite, the docs and
dashboard tooling, and the envtest assets. <code>KUBEBUILDER_ASSETS</code> points at an
<code>etcd</code> + <code>kube-apiserver</code> + <code>kubectl</code> bundle assembled from nixpkgs, so the
envtest-backed tests run offline with nothing to download.</p>
<h2 id="building">Building</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go build -o jaas .
</span></span></code></pre></div><p>The <code>Dockerfile</code> builds the production image. It accepts <code>VERSION</code> and <code>COMMIT</code>
build args:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">docker build -t ghcr.io/metio/jaas:dev .
</span></span><span class="line"><span class="cl">docker build --build-arg <span class="nv">VERSION</span><span class="o">=</span>v1 --build-arg <span class="nv">COMMIT</span><span class="o">=</span>abc123 -t ghcr.io/metio/jaas:dev .
</span></span></code></pre></div><h2 id="regenerating-generated-code">Regenerating generated code</h2>
<p>The CRD manifests under <code>config/crd/bases/</code> and <code>api/v1/zz_generated.deepcopy.go</code>
are produced by <code>controller-gen</code>. Regenerate both after touching <code>api/v1/</code> types,
and commit the result:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command generate
</span></span></code></pre></div><p><code>verify.yml</code>&rsquo;s <code>generated</code> job runs that same command and fails on any diff, so
stale manifests cannot ship. The command is declared in <code>flake.nix</code> and reads
<code>scripts/generate.sh</code> — one definition the gate and this page both point at.</p>
<p><code>controller-gen</code> itself comes from the <code>go.mod</code> <code>tool</code> directive, so its version
lives there and Renovate bumps it.</p>
<h2 id="static-analysis">Static analysis</h2>
<p>golangci-lint is not used. The standalone tools below run directly, both in CI
and in the development shell:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go vet ./...
</span></span><span class="line"><span class="cl">nix develop --command staticcheck ./...     <span class="c1"># config: staticcheck.conf, checks = [&#34;all&#34;]</span>
</span></span><span class="line"><span class="cl">nix develop --command gofumpt -l .          <span class="c1"># empty output means clean; any output is a failure</span>
</span></span><span class="line"><span class="cl">nix develop --command gosec ./...           <span class="c1"># inline #nosec justifications silence false positives</span>
</span></span><span class="line"><span class="cl">nix develop --command govulncheck ./...     <span class="c1"># reachable-from-code advisories only</span>
</span></span><span class="line"><span class="cl">nix develop --command arch-go               <span class="c1"># architecture rules; config: arch-go.yml</span>
</span></span><span class="line"><span class="cl">nix develop --command modernize ./...       <span class="c1"># newer-Go idiom check</span>
</span></span></code></pre></div><h2 id="test-layers">Test layers</h2>
<h3 id="pure-unit-tests">Pure unit tests</h3>
<p>Table-driven tests with no external state. They live next to the code they cover
across <code>internal/...</code> and <code>api/v1/</code>. Several act as drift gates:
<code>conditions_test.go</code> verifies that every <code>Reason*</code> constant has a matching
<code>docs/runbooks/&lt;reason&gt;.md</code>, and <code>TestErrorResponse_StableCodeValues</code> pins the
wire-level <code>ErrCode*</code> strings against accidental rename.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go <span class="nb">test</span> -count<span class="o">=</span><span class="m">1</span> -race -cover ./...
</span></span></code></pre></div><p>To run a single test by name:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go <span class="nb">test</span> -count<span class="o">=</span><span class="m">1</span> -v -run TestName ./internal/handler/
</span></span></code></pre></div><h3 id="envtest-backed-operator-tests">Envtest-backed operator tests</h3>
<p>Files named <code>envtest_*_test.go</code> (in <code>internal/operator/</code>,
<code>internal/webhook/selfsigned/</code>, and <code>main_envtest_test.go</code> at the repo root) boot
a real <code>kube-apiserver</code> and <code>etcd</code> via controller-runtime&rsquo;s <code>envtest</code> package and
run the reconciler, webhook, and full <code>run(...)</code> function against them.</p>
<p>The tests share one apiserver instance per test binary, guarded by a <code>sync.Once</code>,
so the startup cost is paid once. Each test <code>t.Skip</code>s when <code>KUBEBUILDER_ASSETS</code>
is unset — there is no build tag. The development shell exports it, so these
tests run by default; on a host without the assets they silently skip.</p>
<p>The envtest harness sets <code>Config.SkipImpersonation</code> (the only place that setting
is allowed) and defaults <code>MetricsBindAddress</code> to <code>&quot;0&quot;</code> so parallel test cases do
not fight over the metrics port.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go <span class="nb">test</span> -count<span class="o">=</span><span class="m">1</span> -race -cover ./...
</span></span></code></pre></div><h3 id="golden--example-end-to-end-tests">Golden / example end-to-end tests</h3>
<p><code>examples_test.go</code> boots the full binary via <code>runInBackground</code> and asserts HTTP
responses against golden files under <code>testdata/golden/</code>. Comparison is semantic —
both sides are parsed as JSON and compared on the parsed values, so whitespace
and key ordering are irrelevant.</p>
<p>After changing an example or adding a new one, regenerate the golden files:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go <span class="nb">test</span> -update ./...
</span></span></code></pre></div><p>Inspect and commit the diff in <code>testdata/golden/</code>.</p>
<h3 id="fuzz-tests">Fuzz tests</h3>
<p>Fuzz targets in <code>internal/handler/</code>, <code>internal/sources/</code>, and <code>internal/urlguard/</code>
harden the request path, the tar/gzip artifact unpacker, and the SSRF URL/IP
parser against adversarial input. CI exercises their seed corpus as ordinary unit
tests. To fuzz interactively:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go <span class="nb">test</span> -fuzz<span class="o">=</span>FuzzName -fuzztime<span class="o">=</span>30s ./internal/urlguard/
</span></span></code></pre></div><h3 id="benchmarks">Benchmarks</h3>
<p>Throughput benchmarks in <code>internal/eval/</code>, <code>internal/storage/</code>, and
<code>internal/operator/</code> cover reconcile throughput, watch mapping, and the
tenant-client cache. They are baselines, not merge gates. The reconcile benchmark
is envtest-backed and skips without <code>KUBEBUILDER_ASSETS</code>.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go <span class="nb">test</span> -bench<span class="o">=</span>. -benchmem -run<span class="o">=</span>^$ ./internal/operator/
</span></span></code></pre></div><h3 id="kind-operator-smoke-tests">Kind operator smoke tests</h3>
<p>The cluster-level layer runs outside <code>go test</code>. Pure-<code>kubectl</code> bash scenarios in
<code>hack/smoke/</code> run against a real kind cluster via
<code>.github/workflows/kind-smoke.yml</code>. To run a scenario locally against any
reachable cluster, deploy JaaS and invoke the scenario scripts directly:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">hack/smoke/scenario-basic.sh
</span></span></code></pre></div><p>See <a href="/contributing/ci-and-release/">CI and releases</a>
 for how the smoke layer fits
into the two-angle end-to-end strategy.</p>
]]></content><category scheme="https://jaas.projects.metio.wtf/tags/contributing" term="contributing" label="contributing"/><category scheme="https://jaas.projects.metio.wtf/tags/building" term="building" label="building"/><category scheme="https://jaas.projects.metio.wtf/tags/testing" term="testing" label="testing"/></entry></feed>