<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://gohugo.io/" version="0.163.3">Hugo</generator><title type="html">Integrations &amp; tooling on Jsonnet-as-a-Service</title><link href="https://jaas.projects.metio.wtf/integrations/" rel="alternate" type="text/html" title="html"/><link href="https://jaas.projects.metio.wtf/integrations/index.xml" rel="alternate" type="application/rss+xml" title="rss"/><link href="https://jaas.projects.metio.wtf/integrations/atom.xml" rel="self" type="application/atom+xml" title="atom"/><updated>2026-07-02T07:56:36+00:00</updated><author><name>metio.wtf</name></author><id>https://jaas.projects.metio.wtf/integrations/</id><entry><title type="html">Claude Code skill</title><link href="https://jaas.projects.metio.wtf/integrations/claude-code/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://jaas.projects.metio.wtf/contributing/building/?utm_source=atom_feed" rel="related" type="text/html" title="Building and Testing"/><link href="https://jaas.projects.metio.wtf/contributing/ci-and-release/?utm_source=atom_feed" rel="related" type="text/html" title="CI and Releases"/><link href="https://jaas.projects.metio.wtf/integrations/mcp-server/?utm_source=atom_feed" rel="related" type="text/html" title="MCP server"/><id>https://jaas.projects.metio.wtf/integrations/claude-code/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:54:29+02:00</updated><content type="html"><![CDATA[<blockquote>Install the JaaS Claude Code skill so Claude authors and operates JsonnetSnippet and JsonnetLibrary resources with accurate, current field knowledge.</blockquote><p>Install the JaaS skill into <a href="https://claude.com/claude-code">Claude Code</a>
 and
Claude gains working knowledge of JaaS: it authors and edits <code>JsonnetSnippet</code> and
<code>JsonnetLibrary</code> resources, wires snippets to inline files or a Flux source
(<code>GitRepository</code> / <code>OCIRepository</code> / <code>Bucket</code> / <code>ExternalArtifact</code>), chains one
snippet&rsquo;s output into another, pairs JaaS with the grafana-operator or
stageset-controller, configures per-snippet ServiceAccount impersonation, and
calls the HTTP rendering endpoint with top-level arguments and external variables.
The skill points Claude at the live documentation and a compact cheat-sheet, so it
reaches for current fields and defaults instead of guessing.</p>
<p>The skill ships in this repository under <code>skills/jaas/</code> (<code>SKILL.md</code> plus
<code>references/reference.md</code>), packaged as a Claude Code plugin via the manifests in
<code>.claude-plugin/</code>.</p>
<h2 id="install">Install</h2>
<p>Add this repository as a plugin marketplace, then install the plugin:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">/plugin marketplace add metio/jaas
</span></span><span class="line"><span class="cl">/plugin install jaas@jaas
</span></span></code></pre></div><p>The first command registers the <code>jaas</code> marketplace from
<a href="https://github.com/metio/jaas">github.com/metio/jaas</a>
; the second installs the
<code>jaas</code> plugin from it. Claude activates the skill automatically whenever a
repository holds <code>JsonnetSnippet</code> or <code>JsonnetLibrary</code> manifests or JaaS is
otherwise in play.</p>
<h2 id="what-it-grants-claude">What it grants Claude</h2>
<p>The skill&rsquo;s only tools are <code>kubectl</code> and <code>curl</code>, so Claude can inspect snippets
and call the renderer but takes no broader action. It treats
<a href="https://jaas.projects.metio.wtf/">the documentation site</a>
 as the source of truth
— including the machine-readable <code>/llms.txt</code> index and the concatenated
<code>/llms-full.txt</code> — and falls back to the bundled cheat-sheet for quick field
lookups. The guidance covers the authoring gotchas that are easy to get wrong:
exactly one of <code>spec.files</code> or <code>spec.sourceRef</code>, the library alias field being
<code>importPath</code> (not <code>as</code>), single-layer <code>OCIRepository</code> artifacts, and the
ServiceAccount impersonation RBAC each snippet needs.</p>
]]></content><category scheme="https://jaas.projects.metio.wtf/tags/contributing" term="contributing" label="contributing"/><category scheme="https://jaas.projects.metio.wtf/tags/claude" term="claude" label="claude"/><category scheme="https://jaas.projects.metio.wtf/tags/skill" term="skill" label="skill"/><category scheme="https://jaas.projects.metio.wtf/tags/plugin" term="plugin" label="plugin"/></entry><entry><title type="html">Flux Operator (ResourceSet) integration</title><link href="https://jaas.projects.metio.wtf/integrations/flux-operator/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://jaas.projects.metio.wtf/comparisons/flux-operator/?utm_source=atom_feed" rel="related" type="text/html" title="vs Flux Operator ResourceSet"/><link href="https://jaas.projects.metio.wtf/observability/dashboard/?utm_source=atom_feed" rel="related" type="text/html" title="Dashboard"/><id>https://jaas.projects.metio.wtf/integrations/flux-operator/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:54:29+02:00</updated><content type="html"><![CDATA[<blockquote>Template one JsonnetSnippet per input with a Flux Operator ResourceSet so JaaS renders per-cluster or per-tenant content from a single definition.</blockquote><p>Render the same Jsonnet for many clusters or tenants without writing a snippet by
hand for each one: let a <a href="https://fluxoperator.dev/">Flux Operator</a>
 <code>ResourceSet</code>
template a <code>JsonnetSnippet</code> per input, and JaaS renders each instance into its own
<code>ExternalArtifact</code>. The <code>ResourceSet</code> supplies the per-instance values through Go
template substitution; JaaS does the evaluation. There is no <code>ResourceSet</code> field
in the JaaS API — you use the two together: the <code>ResourceSet</code> templates ordinary
<code>JsonnetSnippet</code> CRs, which JaaS reconciles like any other. This needs the Flux
Operator installed alongside JaaS in the operator mode described under
<a href="/operator/operator-mode/">operator mode</a>
.</p>
<p>The worked example below fans the maintained operator dashboard (see
<a href="/observability/dashboard/">Dashboard</a>
) out across a fleet: each cluster gets its
own rendered dashboard, scoped to its own Prometheus and labelled with its own
title.</p>
<h2 id="1-declare-the-per-instance-inputs">1. Declare the per-instance inputs</h2>
<p>A <code>ResourceSetInputProvider</code> of <code>.spec.type</code> <code>Static</code> exports a fixed list of
inputs from <code>.spec.defaultValues</code>. List one entry per cluster, each carrying the
values the dashboard&rsquo;s top-level arguments need:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">fluxcd.controlplane.io/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">ResourceSetInputProvider</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">clusters</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">monitoring</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">type</span><span class="p">:</span><span class="w"> </span><span class="l">Static</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">defaultValues</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">clusters</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">prod</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">datasource</span><span class="p">:</span><span class="w"> </span><span class="l">prometheus-prod</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">selector</span><span class="p">:</span><span class="w"> </span><span class="s1">&#39;cluster=&#34;prod&#34;&#39;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">staging</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">datasource</span><span class="p">:</span><span class="w"> </span><span class="l">prometheus-staging</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">selector</span><span class="p">:</span><span class="w"> </span><span class="s1">&#39;cluster=&#34;staging&#34;&#39;</span><span class="w">
</span></span></span></code></pre></div><p>Other provider types export inputs from a live source instead — Git branches,
pull requests, or OCI artifact tags — and <code>.spec.schedule</code> (with <code>cron</code>,
<code>timeZone</code>, and <code>window</code>) gates when those inputs refresh, so the templated
snippets only change inside the window. The
<a href="https://fluxoperator.dev/docs/resourcesets/time-based-delivery/">time-based delivery guide</a>

covers that pattern.</p>
<h2 id="2-template-a-jsonnetsnippet-per-input">2. Template a JsonnetSnippet per input</h2>
<p>A <code>ResourceSet</code> consumes the provider through <code>.spec.inputsFrom</code>, then templates
its <code>.spec.resources</code> once per resolved input. Each rendered <code>JsonnetSnippet</code>
points at the dashboard <code>OCIRepository</code> and threads the per-cluster values into
<code>spec.tlas</code> with <code>&lt;&lt; inputs.x &gt;&gt;</code> substitution:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">fluxcd.controlplane.io/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">ResourceSet</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">cluster-dashboards</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">monitoring</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceAccountName</span><span class="p">:</span><span class="w"> </span><span class="l">dashboards-tenant</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">inputsFrom</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">ResourceSetInputProvider</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">clusters</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">resources</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">jaas.metio.wtf/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">JsonnetSnippet</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">dashboard-&lt;&lt; inputs.name &gt;&gt;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">monitoring</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">serviceAccountName</span><span class="p">:</span><span class="w"> </span><span class="l">dashboards-tenant</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">OCIRepository</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">jaas-dashboard</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">libraries</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">JsonnetLibrary</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">grafonnet</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">tlas</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">datasource</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">&#34;&lt;&lt; inputs.datasource &gt;&gt;&#34;</span><span class="p">]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">title</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">&#34;JaaS operator — &lt;&lt; inputs.name &gt;&gt;&#34;</span><span class="p">]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">selector</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s1">&#39;&lt;&lt; inputs.selector &gt;&gt;&#39;</span><span class="p">]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">interval</span><span class="p">:</span><span class="w"> </span><span class="l">10m</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">output</span><span class="p">:</span><span class="w"> </span><span class="l">rendered</span><span class="w">
</span></span></span></code></pre></div><p>The <code>ResourceSet</code> creates <code>dashboard-prod</code> and <code>dashboard-staging</code>, each with its
own datasource UID, title, and query selector. JaaS reconciles each snippet
independently and publishes one <code>ExternalArtifact</code> per cluster, named after the
snippet. Point a <code>GrafanaDashboard</code> at each artifact exactly as the
<a href="/observability/dashboard/">Dashboard</a>
 page shows, and the grafana-operator pushes
the per-cluster JSON to Grafana.</p>
<p>The same shape generalises beyond dashboards: any per-tenant or per-environment
Jsonnet — manifest libraries, config documents — fans out the same way. Add a
cluster to the provider&rsquo;s list and the <code>ResourceSet</code> renders one more snippet; no
new snippet YAML by hand.</p>
<p>For where this pattern sits relative to running JaaS or the Flux Operator alone,
see <a href="/comparisons/flux-operator/">JaaS vs Flux Operator ResourceSet</a>
.</p>
]]></content><category scheme="https://jaas.projects.metio.wtf/tags/usage" term="usage" label="usage"/><category scheme="https://jaas.projects.metio.wtf/tags/flux-operator" term="flux-operator" label="flux-operator"/><category scheme="https://jaas.projects.metio.wtf/tags/resourceset" term="resourceset" label="resourceset"/><category scheme="https://jaas.projects.metio.wtf/tags/multi-cluster" term="multi-cluster" label="multi-cluster"/><category scheme="https://jaas.projects.metio.wtf/tags/dashboard" term="dashboard" label="dashboard"/></entry><entry><title type="html">MCP server</title><link href="https://jaas.projects.metio.wtf/integrations/mcp-server/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://jaas.projects.metio.wtf/security/admission-webhook/?utm_source=atom_feed" rel="related" type="text/html" title="Admission webhook"/><link href="https://jaas.projects.metio.wtf/observability/alerting/?utm_source=atom_feed" rel="related" type="text/html" title="Alerting"/><link href="https://jaas.projects.metio.wtf/integrations/claude-code/?utm_source=atom_feed" rel="related" type="text/html" title="Claude Code skill"/><link href="https://jaas.projects.metio.wtf/operator/creating-sources/?utm_source=atom_feed" rel="related" type="text/html" title="Creating source artifacts"/><link href="https://jaas.projects.metio.wtf/observability/dashboard/?utm_source=atom_feed" rel="related" type="text/html" title="Dashboard"/><id>https://jaas.projects.metio.wtf/integrations/mcp-server/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:54:29+02:00</updated><content type="html"><![CDATA[<blockquote>Run the JaaS Model Context Protocol server so an agent renders Jsonnet locally over stdio and reads operator status in-cluster over HTTP.</blockquote><p>JaaS exposes its evaluator and operator status as <a href="https://modelcontextprotocol.io/">Model Context Protocol</a>

tools, so an LLM agent (Claude Code, Claude Desktop, or any MCP client) calls
them directly. There are two modes, each with its own transport:</p>
<ul>
<li><strong>Local rendering over stdio</strong> — <code>jaas mcp</code> wraps the same evaluator the HTTP
endpoint and the operator use, with no cluster and no Kubernetes client. This
is the cluster-free authoring loop.</li>
<li><strong>In-cluster operator introspection over HTTP</strong> — <code>--enable-mcp</code> adds a
streamable-HTTP server inside the operator pod that reads <code>JsonnetSnippet</code>
status as the operator&rsquo;s ServiceAccount.</li>
</ul>
<p>The tools are thin adapters over existing code, so an agent&rsquo;s render is
byte-identical to the HTTP endpoint and the operator.</p>
<h2 id="render-jsonnet-locally">Render Jsonnet locally</h2>
<p>Run the stdio server as a subcommand. Point it at the same library directories
you would pass the HTTP renderer:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">jaas mcp --library-path ./vendor
</span></span></code></pre></div><p>Wire it into an MCP client by launching that command. For Claude Code, add it to
<code>.mcp.json</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;mcpServers&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;jaas&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;command&#34;</span><span class="p">:</span> <span class="s2">&#34;jaas&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;args&#34;</span><span class="p">:</span> <span class="p">[</span><span class="s2">&#34;mcp&#34;</span><span class="p">,</span> <span class="s2">&#34;--library-path&#34;</span><span class="p">,</span> <span class="s2">&#34;./vendor&#34;</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The stdio server registers two tools:</p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Purpose</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>render_jsonnet</code></td>
					<td>Evaluate an inline snippet (with <code>tlas</code> and <code>extVars</code>) and return the resulting JSON. Imports resolve against <code>--library-path</code>, exactly like the HTTP renderer.</td>
			</tr>
			<tr>
					<td><code>validate_jsonnet</code></td>
					<td>Evaluate a snippet and report whether it compiles, returning the full go-jsonnet diagnostic (file and line) on failure without the rendered output.</td>
			</tr>
	</tbody>
</table>
<p>Both accept the same inputs:</p>
<ul>
<li><code>source</code> — the Jsonnet snippet.</li>
<li><code>tlas</code> — top-level arguments as a map of string lists; a single-element list
becomes a string TLA, a multi-element list becomes a JSON-array TLA.</li>
<li><code>extVars</code> — external variables for <code>std.extVar</code>, overlaying any the server was
started with.</li>
</ul>
<p>The flags that shape evaluation:</p>
<table>
	<thead>
			<tr>
					<th>Flag</th>
					<th>Default</th>
					<th>Effect</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>--library-path</code></td>
					<td><em>(none)</em></td>
					<td>Directory searched for <code>import</code> resolution; repeatable, rightmost wins.</td>
			</tr>
			<tr>
					<td><code>--ext-var KEY=VALUE</code></td>
					<td><em>(none)</em></td>
					<td>Server-level external variable; repeatable. Overlays <code>JAAS_EXT_VAR_*</code> env on conflict.</td>
			</tr>
			<tr>
					<td><code>--max-stack</code></td>
					<td><code>500</code></td>
					<td>Maximum call-stack depth; <code>0</code> uses go-jsonnet&rsquo;s default.</td>
			</tr>
			<tr>
					<td><code>--evaluation-timeout</code></td>
					<td><code>5s</code></td>
					<td>Per-evaluation bound; <code>0</code> disables it.</td>
			</tr>
			<tr>
					<td><code>--log-level</code> / <code>--log-format</code></td>
					<td><code>info</code> / <code>json</code></td>
					<td>Diagnostics go to stderr — stdout carries the protocol, so logs never corrupt it.</td>
			</tr>
	</tbody>
</table>
<p>Unlike the broadly-reachable HTTP path, this server returns the full go-jsonnet
diagnostic on a failed render: it is owner-facing, like the operator status
condition. See <a href="/rendering/evaluation-and-security/">Evaluation and security</a>
.</p>
<h2 id="inspect-the-operator-in-cluster">Inspect the operator in-cluster</h2>
<p>When JaaS runs as an <a href="/operator/operator-mode/">operator</a>
, add <code>--enable-mcp</code> to serve
the read tools over streamable HTTP from the same pod:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">jaas --enable-flux-integration --enable-mcp --mcp-bind-address :8084
</span></span></code></pre></div><p><code>--enable-mcp</code> requires <code>--enable-flux-integration</code> — the tools introspect
operator resources, so there is nothing to serve without the operator. The
server binds <code>--mcp-bind-address</code> (default <code>:8084</code>, chosen to avoid the jsonnet
<code>:8080</code>, management <code>:8081</code>, storage <code>:8082</code>, and metrics <code>:8083</code> ports) and
reads as the operator&rsquo;s ServiceAccount, so it can never see more than the
operator&rsquo;s own RBAC allows. The local render tools above are served here too.</p>
<p>Reach it from your machine with a port-forward:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl --namespace jaas-system port-forward deploy/jaas 8084:8084
</span></span></code></pre></div><p>The in-cluster server adds these read tools:</p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Purpose</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>list_snippets</code></td>
					<td>List <code>JsonnetSnippet</code> resources with their Ready status, reason, suspend state, revision, and artifact URL. Omit the namespace to list across every namespace the operator can read.</td>
			</tr>
			<tr>
					<td><code>get_snippet</code></td>
					<td>One snippet&rsquo;s full status: the Ready condition (status, reason, message), the per-reason <a href="/runbooks/">runbook</a>
 URL, suspend state, revision, artifact URL, and the retained revision history.</td>
			</tr>
			<tr>
					<td><code>diff_revisions</code></td>
					<td>A per-file unified diff of a snippet&rsquo;s published output between two retained revisions. Omit the revisions to compare the two most recent in <code>status.history</code>; pass <code>from</code>/<code>to</code> (sha256) to diff specific ones. Reads the artifacts straight from the operator&rsquo;s store. Needs <code>spec.history</code> greater than 1 to retain a second revision.</td>
			</tr>
	</tbody>
</table>
<h2 id="gated-mutations">Gated mutations</h2>
<p>The server is read-only by default. Add <code>--mcp-allow-mutations</code> (which requires
<code>--enable-mcp</code>) to also expose write tools:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">jaas --enable-flux-integration --enable-mcp --mcp-allow-mutations
</span></span></code></pre></div><table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Effect</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>reconcile_snippet</code></td>
					<td>Stamp the <code>reconcile.fluxcd.io/requestedAt</code> annotation to request an immediate reconcile — the same trigger as <code>flux reconcile</code>.</td>
			</tr>
			<tr>
					<td><code>suspend_snippet</code></td>
					<td>Set <code>spec.suspend=true</code> so the operator stops re-rendering the snippet.</td>
			</tr>
			<tr>
					<td><code>resume_snippet</code></td>
					<td>Clear <code>spec.suspend</code> to resume reconciliation.</td>
			</tr>
	</tbody>
</table>
<p>These act on the <code>JsonnetSnippet</code> resource as the operator&rsquo;s ServiceAccount, so
they can never exceed the operator&rsquo;s own RBAC. Keep them off unless you intend
the agent to drive reconciliation, and have your MCP client confirm each call.</p>
]]></content><category scheme="https://jaas.projects.metio.wtf/tags/mcp" term="mcp" label="mcp"/><category scheme="https://jaas.projects.metio.wtf/tags/claude" term="claude" label="claude"/><category scheme="https://jaas.projects.metio.wtf/tags/operator" term="operator" label="operator"/><category scheme="https://jaas.projects.metio.wtf/tags/rendering" term="rendering" label="rendering"/></entry></feed>